#include "states/GamesState.hpp"

GameState::GameState(sf::RenderWindow* window) {
    this->window = window;
}

void GameState::draw(sf::RenderTarget& target, sf::RenderStates states) const {

}

void GameState::update() {

}